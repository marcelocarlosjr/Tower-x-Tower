using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddTowerToInventory : MonoBehaviour
{
    private InventoryController inventory;
    void Start()
    {
        //inventory = GameObject.FindGameObjectWithTag("Tower").GetComponent<InventoryController>();
        //inventory.towers.Add(this.gameObject);
    }
}
